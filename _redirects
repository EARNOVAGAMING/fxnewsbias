# Force a single canonical URL for the homepage so Google doesn't see
# /index.html and / as two pages. 301 = permanent (passes SEO weight).
/index.html  /  301

# www -> apex redirect handled at Cloudflare DNS / page rule level
# (Worker static assets only allows relative URLs, not cross-host redirects)
